[providers]
active = ["sqlite"]
fuzz_threshold = 80
fuzz_max_candidates = 20

[scan]
ignore_patterns = ["*.min.js", "vendor/**", "docs/**", "tests/**", "README.md"]

[plugins]
local = ".zmem/plugins.py"
enabled = ["core"]

[instances]
